function HeroHeading() {
    return (
        <p id="hero-heading" className="backdrop-blur-sm text-[white]">
            There's <span className="italic">always </span>more to learn.
        </p>
    );
}

export default HeroHeading;
